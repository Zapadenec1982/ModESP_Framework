# 01 — Architecture

**Status:** placeholder. Filled у Step 1+ (modr_format) і дозаповнюється до Step 14 (engine completion).

## Заповнюється

- High-level architecture діаграма (build-time pipeline + runtime)
- Components: SequenceEngine, ActionRegistry, ContinuousRegistry, ModrLoader, NvsToken, ResourceArbiter
- Lifecycle from manifest authoring до runtime execution
- Cross-references: 02 (binary format), 03 (API), 04 (state machines), 09 (manifest)
