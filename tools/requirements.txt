# Python tooling dependencies для ModESP build pipeline.
# Install: pip install -r tools/requirements.txt
#
# Used by:
#   tools/generate_ui.py        — manifest validation
#   tools/compile_scenario.py   — JSON Schema validation
#   tools/tests/                — pytest suite
#
# REQUIRED for `idf.py build`: the root CMakeLists.txt runs generate_ui.py and
# compile_scenario.py at configure time, and compile_scenario.py needs jsonschema.
# Install into the ESP-IDF Python env: pip install -r tools/requirements.txt

jsonschema>=4.0,<5.0
pytest>=8.0,<10.0

# Optional: Hypothesis для property tests (Step 2b)
# hypothesis>=6.0,<7.0
